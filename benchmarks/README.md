# Benchmarks

Each benchmark must be computed or sourced independently of the prediction implementation. Record its derivation, units, assumptions, reference, version, and hash. A benchmark copied from the same code path as the prediction is not independent.

