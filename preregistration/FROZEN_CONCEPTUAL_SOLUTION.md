# Frozen Conceptual Solution

## M-only starting point

`M = (F <-> F)^infinity`

The tested configuration is not an isolated object. Its distinguishable evolution is formed by reciprocal differences, exchanges, constraints, and residual consequences within a declared field.

## Necessary consequences

1. Delimiting a test system does not make it isolated.
2. A system-level transition cannot be assigned as an intrinsic property of one isolated part.
3. Coordinate duration cannot be assumed to drive the transformation if duration is the quantity being reconstructed.
4. A candidate intrinsic progression must be derived from the complete declared configuration path, not from the desired terminal time.
5. The scale conversion may be applied only after the intrinsic path has been generated.
6. A benchmark may test the prediction but may not define the path that produces it.

## Frozen candidate relation

The hypothesis under test remains:

```text
Delta t_pred = kappa * Delta tau
```

with `kappa = 3.19275 s/arc-unit` treated as a historical frozen claim, not recalibrated on System C or System D.

## Non-negotiable failure rule

If the operational definition of state, relation, path increment, boundary crossing, or terminal event is ambiguous, the system produces **no prediction**. Choosing among definitions by proximity to a benchmark is prohibited.

