# System C — blocked evidence package

Advertised description: discrete seven-node cluster collapse.

Advertised values are recorded in `evidence/claims.yaml` only as leads. This system must not be implemented by working backward from 0.12475 s or 0.12480 s.

Required original material:

1. interaction/adjacency matrix;
2. state definition and equations;
3. initial stress/energy/state values;
4. update or integration rule;
5. precise macro-collapse and stall event definition;
6. numerical tolerances and seed, if any;
7. provenance of the 0.12480 s benchmark;
8. proof that no System C value was used to tune `kappa` or another coefficient.

