# System D — blocked evidence package

Advertised description: singular radial gravitational free fall with benchmark pi/4 seconds.

Advertised values are recorded in `evidence/claims.yaml` only as leads. This system must not be implemented by working backward from 0.785382 s or pi/4.

Required original material:

1. masses and unit normalization;
2. release distance and velocity;
3. governing relational equations;
4. exact definition of intrinsic increment `d_tau`;
5. collision/event condition at the singular limit;
6. integration method and tolerances;
7. analytic derivation and provenance of the pi/4 benchmark;
8. proof that no System D value was used for calibration or retuning.

